<div class="filters-box frame-default frame-blue">
  <h2>FILTRES</h2>
  <%= simple_form_for :search, url: aupairs_path, method: :get do |f| %>
    <div class="container-fluid text-left">
      <h3>VOTRE AU PAIR DOIT PARLER :</h3>
      <div class="row">
      <%= f.collection_radio_buttons(
        :country, Country.all, :id, :name, item_wrapper_class: "filter-radio col-xs-6 text-left") %>
      </div>
    </div>
    <div class="form-group text-left">
      <h3>LE SÉJOUR COMMENCE :</h3>
      <div class='input-group date' id='datetimepicker-standar'>
        <!-- <input type='text' class="input-group-addon form-control" /> -->
        <%= f.input :date, label: false, input_html: { class: "input-group-addon form-control" } %>
      </div>
    </div>
    <%= f.button :submit, "RECHERCHER", class: "btn btn-black" %>
  <% end %>
</div>
